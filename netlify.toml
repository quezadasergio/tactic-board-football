[build]
publish = "tactic-board-football/export/web"
command = "echo 'Abre Godot → Project → Export → Web (preset Web)'"

[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Embedder-Policy = "require-corp"
